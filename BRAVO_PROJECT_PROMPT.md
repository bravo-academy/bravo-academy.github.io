# 🎓 برافو أكاديمي · Bravo Academy — قناة تعليمية لكل الابتدائي (من الصفر، مستقل)

أنت وكيل برمجة على Arena.ai. المستودع الحالي **جديد وفارغ** (تحت **حساب GitHub مخصّص** اسمه `bravo-academy`، والمستودع اسمه `bravo-academy.github.io` — المعرّف `bravo-academy/bravo-academy.github.io`).
**لا تعتمد على أي ملف أو مشروع خارجي.** كل شيء يُبنى/يُولَّد من الصفر داخل هذا المستودع. التخاطب بالعربية (لهجة تونسية مقبولة).

## 🎯 الرؤية
قناة تعليمية يوتيوب **لكل المرحلة الابتدائية التونسية (السنة 1→6 أساسي)**، تتبع **البرنامج البيداغوجي الرسمي** لكل مادة في كل مستوى. **حلقة كل يوم** ضمن رزنامة أيام أسبوع ثابتة. محتوى هرمي: **مستوى ← مادة ← درس (حلقة)**.

---

## 1) هوية القناة
- **الاسم:** برافو أكاديمي – Bravo Academy
- **المعرف:** `@BravoAcademyTN`
- **البلد:** 🇹🇳 تونس · **مصنوع للأطفال:** ✅ (على مستوى القناة)
- **اللغة:** العربية (أساسي) + الفرنسية
- **فكرة الاسم:** «برافو» = كلمة التشجيع في نهاية كل حلقة.

## 2) المستويات والمواد (حسب المنهاج الرسمي التونسي)
**6 مستويات:** `g1` السنة الأولى … `g6` السنة السادسة أساسي.

**المواد الأكاديمية (تُنتج بحسب المستوى كما يحدّدها المنهاج الرسمي):**
| المادة | الرمز | المستويات |
|---|---|---|
| اللغة العربية | `arabic` | g1→g6 |
| الرياضيات | `math` | g1→g6 |
| التربية العلمية (إيقاظ علمي) | `science` | g1→g6 |
| اللغة الفرنسية | `french` | g3→g6 (تبدأ من الثالثة) |
| التربية الإسلامية | `islamic` | g1→g6 |
| التربية المدنية | `civic` | g1→g6 |
| التاريخ والجغرافيا | `history-geo` | g4→g6 |

لكل زوج (مستوى، مادة): **سلسلة دروس مرتّبة حسب البرنامج البيداغوجي الرسمي** (انظر `curriculum/` في §5). كل درس = حلقة واحدة.

## 3) 🗓 رزنامة النشر اليومية (كل يوم = مادة ثابتة لكل المستويات)
| اليوم | المادة | المحتوى |
|---|---|---|
| الأحد | رياضيات 📐 | الدرس التالي في الرياضيات لكل المستويات |
| الإثنين | لغة عربية ✍️ | درس عربية لكل المستويات |
| الثلاثاء | تربية علمية 🔬 | درس إيقاظ لكل المستويات |
| الأربعاء | لغة فرنسية 🇫🇷 | درس فرنسية للمستويات g3→g6 |
| الخميس | تربية إسلامية + مدنية 🕌 | لكل المستويات |
| الجمعة | تاريخ وجغرافيا 🗺️ | للمستويات g4→g6 |
| السبت | مراجعة + كويز 🏆 | كويز أسبوعي لكل المستويات |

- وقت النشر الموحّد: **18:00 بتوقيت تونس** (يوتيوب)، و**19:30** نسخة فيسبوك/ريلز.
- كل درس يُنتج: فيديو 1920×1080 + Short عمودي + صورة مصغّرة + SRT.

## 4) 📺 تنظيم يوتيوب
- **قسم (Section) لكل مستوى:** «السنة الأولى أساسي» … «السنة السادسة أساسي» (6 أقسام).
- **داخل كل قسم: قائمة تشغيل لكل مادة:** مثل «رياضيات — الثالثة أساسي».
- **داخل كل قائمة: الحلقات مرتّبة بترتيب الدروس** (حسب `curriculum/`).
- **التريلر:** Short درس افتتاحي لكل مستوى.

## 5) بنية المستودع
انظر الشجرة في رسالة التأسيس. المخرجات الكبيرة (`*.mp4`) في `.gitignore`.

## 6–14) محرّك المحتوى، الهوية البصرية، الاستوديو، CHANNEL/SCHEDULE، Pages، معايير القبول، خارطة يوتيوب API
تُطبَّق كما في برومبت الماستر الأصلي حرفيًا (ماسكت نجمة + فجوة ≥130px، `render.py`، g3 نموذجي، نشر يدوي أولًا).
